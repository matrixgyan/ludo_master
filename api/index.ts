// @ts-ignore - _bundle.js is generated during build
import { app } from './_bundle.js';

// Vercel Serverless Function entrypoint
// Routes all /api/* requests to the Express application
export default app;
